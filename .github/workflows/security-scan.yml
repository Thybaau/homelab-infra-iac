name: Security Scan

on:
  push:
    branches:
      - main
      - develop
  pull_request:
    branches:
      - main
      - develop
  workflow_dispatch:
  schedule:
    - cron: "0 2 * * 1" # Tous les lundis à 2h du matin

permissions:
  contents: read
  security-events: write
  actions: read
  pull-requests: write

jobs:
  secret-scan:
    name: Scan des Secrets
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6.1.0
        with:
          fetch-depth: 0 # Récupère tout l'historique pour scanner tous les commits

      - name: Block sensitive files
        run: |
          echo "🔍 Vérification qu'aucun fichier sensible n'est présent dans le repo..."
          FORBIDDEN=$(git ls-files | grep -E '\.tfvars$|\.tfstate$|\.tfstate\.' | grep -v '\.example$' || true)
          if [ -n "$FORBIDDEN" ]; then
            echo "❌ ERREUR: Fichiers sensibles détectés dans le repo !"
            echo "$FORBIDDEN"
            echo ""
            echo "Ces fichiers ne doivent JAMAIS être commités. Retirez-les avec:"
            echo "  git rm --cached <fichier>"
            exit 1
          fi
          echo "✅ Aucun fichier sensible commité"

      - name: TruffleHog Secret Scan
        uses: trufflesecurity/trufflehog@8cd2fdddd14a513cce30888e975d09233a61705c # v3.88.8
        with:
          path: ./
          extra_args: --debug --only-verified

      - name: Gitleaks Secret Scan
        uses: gitleaks/gitleaks-action@e0c47f4f8be36e29cdc102c57e68cb5cbf0e8d1e # v3
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITLEAKS_LICENSE: ${{ secrets.GITLEAKS_LICENSE }}

  terraform-security:
    name: Terraform Security Scan
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6.1.0

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@dfe3c3f87815947d99a8997f908cb6525fc44e9e # v4.0.1
        with:
          terraform_version: 1.5.0

      - name: Terraform Format Check
        run: terraform fmt -check -recursive
        continue-on-error: true

      - name: Terraform Init
        run: terraform init -backend=false

      - name: Terraform Validate
        run: terraform validate

      - name: tfsec Security Scan
        run: |
          docker run --rm -v $(pwd):/src aquasec/tfsec:latest \
            /src \
            --format sarif \
            --minimum-severity MEDIUM \
            --soft-fail > tfsec-results.sarif || true

      - name: Upload tfsec Results
        uses: actions/upload-artifact@b7c566a772e6b6bfb58ed0dc250532a479d7789f # v6.0.0
        if: always() && hashFiles('tfsec-results.sarif') != ''
        with:
          name: tfsec-results
          path: tfsec-results.sarif
          retention-days: 90

      - name: Checkov Terraform Scan
        uses: bridgecrewio/checkov-action@9b70310bcd306d11740313070b940167d6b23085 # v12
        with:
          directory: .
          framework: terraform
          output_format: cli
          soft_fail: true
          download_external_modules: false

  dependency-scan:
    name: Dependency Vulnerability Scan
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6.1.0

      - name: Run Trivy vulnerability scanner
        uses: aquasecurity/trivy-action@ed142fd0673e97e23eac54620cfb913e5ce36c25 # v0.36.0
        with:
          scan-type: "config"
          scan-ref: "."
          format: "sarif"
          output: "trivy-results.sarif"
          severity: "CRITICAL,HIGH,MEDIUM"

      - name: Upload Trivy Results
        uses: actions/upload-artifact@b7c566a772e6b6bfb58ed0dc250532a479d7789f # v6.0.0
        if: always()
        with:
          name: trivy-results
          path: trivy-results.sarif
          retention-days: 90

  workflow-security:
    name: GitHub Actions Workflow Security
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6.1.0

      - name: Validate GitHub Actions Workflows
        run: |
          echo "🔍 Vérification des workflows GitHub Actions..."

          # Vérifier que tous les workflows utilisent des versions pinnées
          echo "Vérification des versions d'actions pinnées..."
          if grep -r "uses:.*@main\|uses:.*@master" .github/workflows/ --exclude="security-scan.yml"; then
            echo "⚠️  ATTENTION: Certaines actions utilisent @main ou @master au lieu de versions spécifiques"
            echo "Recommandation: Utilisez des versions spécifiques (ex: @v3) pour la reproductibilité"
          else
            echo "✅ Toutes les actions utilisent des versions spécifiques"
          fi

          # Vérifier l'utilisation de secrets
          echo ""
          echo "Vérification de l'utilisation des secrets..."
          if grep -r "secrets\." .github/workflows/ | grep -v "GITHUB_TOKEN"; then
            echo "✅ Secrets détectés dans les workflows (utilisation correcte)"
          fi

          # Vérifier les permissions des workflows
          echo ""
          echo "Vérification des permissions des workflows..."
          for workflow in .github/workflows/*.yml; do
            if ! grep -q "permissions:" "$workflow"; then
              echo "⚠️  $workflow: Aucune permission explicite définie"
            fi
          done

  security-report:
    name: Générer Rapport de Sécurité
    runs-on: ubuntu-latest
    needs: [secret-scan, terraform-security, dependency-scan, workflow-security]
    if: always()

    steps:
      - name: Checkout code
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6.1.0

      - name: Generate Security Report
        run: |
          echo "# 🔒 Rapport de Sécurité" > security-report.md
          echo "" >> security-report.md
          echo "**Date**: $(date)" >> security-report.md
          echo "**Commit**: ${{ github.sha }}" >> security-report.md
          echo "**Branch**: ${{ github.ref_name }}" >> security-report.md
          echo "" >> security-report.md

          echo "## Résultats des Scans" >> security-report.md
          echo "" >> security-report.md
          echo "- ✅ Scan des secrets: ${{ needs.secret-scan.result }}" >> security-report.md
          echo "- ✅ Scan Terraform: ${{ needs.terraform-security.result }}" >> security-report.md
          echo "- ✅ Scan des dépendances: ${{ needs.dependency-scan.result }}" >> security-report.md
          echo "- ✅ Scan des workflows: ${{ needs.workflow-security.result }}" >> security-report.md
          echo "" >> security-report.md

          echo "## Recommandations" >> security-report.md
          echo "" >> security-report.md
          echo "1. Vérifiez les résultats dans l'onglet Security de GitHub" >> security-report.md
          echo "2. Corrigez les vulnérabilités critiques et hautes en priorité" >> security-report.md
          echo "3. Ne commitez jamais de secrets en dur dans le code" >> security-report.md
          echo "4. Utilisez GitHub Secrets pour les informations sensibles" >> security-report.md
          echo "5. Maintenez Terraform et les providers à jour" >> security-report.md

          cat security-report.md

      - name: Upload Security Report
        uses: actions/upload-artifact@b7c566a772e6b6bfb58ed0dc250532a479d7789f # v6.0.0
        with:
          name: security-report
          path: security-report.md
          retention-days: 90

      - name: Comment PR with Security Status
        if: github.event_name == 'pull_request'
        uses: actions/github-script@d746ffe35508b1917358783b479e04febd2b8f71 # v9.0.0
        with:
          script: |
            const fs = require('fs');
            const report = fs.readFileSync('security-report.md', 'utf8');

            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: report
            });
